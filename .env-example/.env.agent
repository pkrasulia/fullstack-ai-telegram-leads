# ------------------------------------------------------------------------------
# Используемая платформа для генеративного ИИ
# 0 — использовать Google Gemini Developer API
# 1 — использовать Vertex AI
GOOGLE_GENAI_USE_VERTEXAI=0

# ------------------------------------------------------------------------------
# Google Gemini Developer API Key
# Требуется, если GOOGLE_GENAI_USE_VERTEXAI=0.
# Этот ключ нужен для доступа к API Google Gemini.
# ⚠️ Никогда не публиковать публично. В проде хранить через секреты.
GOOGLE_API_KEY=AIzaSyDEmZPurqWSFbXeoZ4ZJMwIUXpYApNENIc

# ------------------------------------------------------------------------------
# Конфигурация Vertex AI backend
# Регион, в котором будут создаваться ресурсы Vertex AI
# Используется только если GOOGLE_GENAI_USE_VERTEXAI=1
GOOGLE_CLOUD_LOCATION=us-central1

# ------------------------------------------------------------------------------
# Режим работы приложения
# Определяет конфигурацию: development / production / test
NODE_ENV=development

# ------------------------------------------------------------------------------
# Service Account (бот / агент)
# Эти креды используются агентом (ботом) для авторизации в сервисном аккаунте.
# Агент логинится через /api/v1/auth/login с этим email/password и получает access token,
# который используется для вызова защищённых endpoint'ов backend.
#
# ⚠️ Внимание:
# - Пароль хранится открытым только в .env (и в secrets менеджере в проде)
# - В базе данных пароль хранится в захешированном виде
# - Доступ имеют только агент и backend (для проверки токена)
# ------------------------------------------------------------------------------
SERVICE_ACCOUNT_LOGIN=service@example.com
SERVICE_ACCOUNT_PASSWORD=secret