
# Telegram Bot Token
TELEGRAM_BOT_TOKEN=

# Backend
BACKEND_BASE_URL=http://backend:4343/api/v1

# ------------------------------------------------------------------------------
# Service Account (бот / агент)
# Эти креды нужны, чтобы backend (через сиды) создал сервисного пользователя
# с ролью "Service". Тот же login/password используются агентом (ботом),
# чтобы авторизоваться через /auth/login и получить access token.
# 
# ⚠️ Внимание:
# - Пароль хранится открытым только в .env (и в secrets менеджере в проде).
# - В базе данных он будет захеширован.
# - Доступ имеют только backend (для сидов) и агент (для логина).
# ------------------------------------------------------------------------------
SERVICE_ACCOUNT_LOGIN=service@example.com
SERVICE_ACCOUNT_PASSWORD=secret